# Instructions

You should add to /songs_books
    
    /{psalmBookName}
      /{songNumber}.jpg
      /{songNumber}.jpg
      /{songNumber}.jpg

and then call `importSongImages(psalmsBookId: ID!)`



